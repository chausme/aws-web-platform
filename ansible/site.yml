- name: Setup site
  hosts: web
  become: true

  roles:
    - site
    - php_site
    - nginx_site
    - role: wp_cli
      when: site.app == "wordpress"
    - role: wp_site
      when: site.app == "wordpress"
