- name: Download WP core
  ansible.builtin.command: /usr/local/bin/wp core download --path=/sites/{{ site.domain }}/files
  args:
    creates: "/sites/{{ site.domain }}/files/wp-config-sample.php"
  become: true
  become_user: "{{ site.user }}"
