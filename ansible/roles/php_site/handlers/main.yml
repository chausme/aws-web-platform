- name: Reload php-fpm # noqa: no-changed-when
  ansible.builtin.shell: |
    pkill -HUP -o php-fpm{{ site.php_version }} || php-fpm{{ site.php_version }} -D
  args:
    executable: /bin/bash
  changed_when: false
